services:
  gateway:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - "8000:8000"
    environment:
      RAZORPAY_WEBHOOK_SECRET: ${RAZORPAY_WEBHOOK_SECRET:?set a webhook secret}
      ADMIN_TOKEN: ${ADMIN_TOKEN:?set an admin token}
